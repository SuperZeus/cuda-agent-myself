{
  "name": "standard-eval",
  "python": "python3",
  "device_preference": "cuda",
  "torch_compile_enabled": true,
  "workspace_root": "logs/runs",
  "default_timeout_sec": 600,
  "notes": "Use this profile as the locked benchmark environment contract."
}
